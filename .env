# Environment variables for the dashboard application

# Backend API URL
# This will be used by the frontend to communicate with the backend
# Default is localhost:3001, but can be overridden for different environments
VITE_BACKEND_URL=http://localhost:3001

# Other potential environment variables can be added here
# VITE_API_TIMEOUT=5000
# VITE_DEBUG_MODE=false